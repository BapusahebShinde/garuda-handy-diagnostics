<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Internal storage -->
  <files-path
      name="internal_files"
      path="." />

  <!-- Cache -->
  <cache-path
      name="cache"
      path="." />

  <!-- External app-specific storage -->
  <external-files-path
      name="external_files"
      path="." />

  <!-- External cache -->
  <external-cache-path
      name="external_cache"
      path="." />

  <!-- Full external storage (use carefully) -->
  <external-path
      name="external"
      path="." />

</paths>